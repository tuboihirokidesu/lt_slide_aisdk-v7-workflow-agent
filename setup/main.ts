import { defineAppSetup } from '@slidev/types'
import '../styles/index.css'

export default defineAppSetup(({ app, router }) => {
  // No additional Vue setup needed; styles are imported globally above.
})
